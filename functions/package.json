{
  "name": "sakaynow-buenatoda-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "tsc --noEmit",
    "test:unit": "npm run build && node --test test/ride_conversations.test.mjs",
    "test:rules": "npm run build && node --test --test-concurrency=1 test/admin_roles.rules.test.mjs test/super_admin_migration.test.mjs",
    "migrate:super-admin": "npm run build && node lib/migrate_super_admin.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@types/node": "^22.10.2",
    "firebase": "^11.0.0",
    "typescript": "^5.7.2"
  }
}
